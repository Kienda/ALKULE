# Local development works without these values. Set them in Railway production.
SESSION_SECRET=replace-with-at-least-32-random-characters
# Mount a Railway volume and point this at its mount directory for durable JSON data.
ALKULE_DATA_DIR=/data
# Railway supplies PORT automatically.
PORT=3000
