"use client";import{FormEvent,useState}from"react";export default function NewsletterForm(){const[message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const form=e.currentTarget,email=new FormData(form).get('email');try{const response=await fetch('/api/newsletter',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const data=await response.json();setMessage(response.ok?data.message:data.error);if(response.ok)form.reset()}catch{setMessage('Unable to connect. Please try again.')}finally{setBusy(false)}}return <form onSubmit={submit} className="container-page max-w-2xl text-center"><h2 className="section-title">Stay in the loop</h2><p className="mt-3 text-muted">Receive updates about courses and cultural learning resources.</p><div className="mx-auto mt-6 flex max-w-lg flex-col gap-3 sm:flex-row"><label className="sr-only" htmlFor="newsletter">Email address</label><input name="email" id="newsletter" required type="email" className="min-h-12 flex-1 rounded-lg border border-border px-4" placeholder="Email address"/><button disabled={busy} className="btn-primary disabled:opacity-60">{busy?'Joining…':'Join updates'}</button></div><p role="status" className="mt-3 min-h-6 text-sm">{message}</p></form>}
