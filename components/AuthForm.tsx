"use client";import{FormEvent,useState}from"react";export default function AuthForm({mode}:{mode:"login"|"signup"}){const[show,setShow]=useState(false);const[message,setMessage]=useState('');function submit(e:FormEvent){e.preventDefault();setMessage('This Phase 1 form is ready for validation, but authentication is not connected.')}return <form onSubmit={submit} className="card mx-auto max-w-md space-y-5 p-7" noValidate>{mode==='signup'&&<label className="block font-bold">Name<input required autoComplete="name" className="mt-2 min-h-12 w-full rounded-lg border border-border px-3"/></label>}<label className="block font-bold">Email address<input required type="email" autoComplete="email" className="mt-2 min-h-12 w-full rounded-lg border border-border px-3"/></label><label className="block font-bold">Password<span className="mt-2 flex"><input required minLength={8} type={show?'text':'password'} autoComplete={mode==='login'?'current-password':'new-password'} aria-describedby="password-help" className="min-h-12 min-w-0 flex-1 rounded-s-lg border border-border px-3"/><button type="button" onClick={()=>setShow(v=>!v)} className="min-h-12 rounded-e-lg border border-s-0 border-border px-4" aria-pressed={show}>{show?'Hide':'Show'}</button></span><span id="password-help" className="mt-2 block text-xs font-normal text-muted">Use at least 8 characters. No credentials are submitted in this frontend preview.</span></label><button className="btn-primary w-full" type="submit">{mode==='login'?'Log in':'Create free account'}</button><p className="min-h-6 text-sm text-indigo-brand" role="status">{message}</p></form>}
