"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";
export default function AuthForm({mode}:{mode:"login"|"signup"}){const[show,setShow]=useState(false),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage('');const form=new FormData(e.currentTarget);try{const response=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:form.get('name'),email:form.get('email'),password:form.get('password')})});const data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to continue.');setMessage(mode==='login'?'Signed in. Opening your dashboard…':'Account created. Opening your dashboard…');router.push('/dashboard');router.refresh()}catch(error){setMessage(error instanceof Error?error.message:'Unable to continue.')}finally{setBusy(false)}}return <form onSubmit={submit} className="card mx-auto max-w-md space-y-5 p-7">{mode==='signup'&&<label className="block font-bold">Name<input name="name" required minLength={2} autoComplete="name" className="mt-2 min-h-12 w-full rounded-lg border border-border px-3"/></label>}<label className="block font-bold">Email address<input name="email" required type="email" autoComplete="email" className="mt-2 min-h-12 w-full rounded-lg border border-border px-3"/></label><label className="block font-bold">Password<span className="mt-2 flex"><input name="password" required minLength={8} type={show?'text':'password'} autoComplete={mode==='login'?'current-password':'new-password'} aria-describedby="password-help" className="min-h-12 min-w-0 flex-1 rounded-s-lg border border-border px-3"/><button type="button" onClick={()=>setShow(v=>!v)} className="min-h-12 rounded-e-lg border border-s-0 border-border px-4" aria-pressed={show}>{show?'Hide':'Show'}</button></span><span id="password-help" className="mt-2 block text-xs font-normal text-muted">Use at least 8 characters.</span></label><button disabled={busy} className="btn-primary w-full disabled:opacity-60" type="submit">{busy?'Please wait…':mode==='login'?'Log in':'Create free account'}</button><p className="min-h-6 text-sm text-indigo-brand" role="status">{message}</p></form>}
