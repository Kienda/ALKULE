"use client";import{useMemo,useState}from"react";import{COURSES}from"@/data/courses";import CourseCard from"./CourseCard";export default function CourseCatalog(){const[q,setQ]=useState('');const[level,setLevel]=useState('All');const[format,setFormat]=useState('All');const[filters,setFilters]=useState(false);const shown=useMemo(()=>COURSES.filter(c=>(!q||`${c.title} ${c.description}`.toLowerCase().includes(q.toLowerCase()))&&(level==='All'||c.level===level)&&(format==='All'||c.format===format)),[q,level,format]);const controls=<><label className="block text-sm font-bold">Level<select value={level} onChange={e=>setLevel(e.target.value)} className="mt-2 min-h-11 w-full rounded-lg border border-border bg-white px-3"><option>All</option><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></label><label className="block text-sm font-bold">Format<select value={format} onChange={e=>setFormat(e.target.value)} className="mt-2 min-h-11 w-full rounded-lg border border-border bg-white px-3"><option>All</option><option>Self-paced</option><option>Live</option></select></label><label className="block text-sm font-bold">Language<select className="mt-2 min-h-11 w-full rounded-lg border border-border bg-white px-3"><option>All sample languages</option><option>English</option><option>French</option><option>Fulfulde</option></select></label><p className="text-xs text-muted">Access examples include free, paid, and subscription states. No checkout is connected.</p></>;return <div><div className="flex gap-3"><label className="sr-only" htmlFor="course-search">Search courses</label><input id="course-search" value={q} onChange={e=>setQ(e.target.value)} className="min-h-12 flex-1 rounded-xl border border-border bg-white px-4" placeholder="Search sample courses"/><button className="btn-secondary lg:hidden" onClick={()=>setFilters(v=>!v)} aria-expanded={filters}>Filters</button></div><div className="mt-8 grid gap-8 lg:grid-cols-[14rem_1fr]"><aside className={`${filters?'block':'hidden'} card h-fit space-y-5 p-5 lg:block`} aria-label="Course filters">{controls}</aside><div>{shown.length?<div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{shown.map(c=><CourseCard key={c.id} course={c}/>)}</div>:<div className="card p-10 text-center"><h2 className="text-xl font-bold">No sample courses match</h2><p className="mt-2 text-muted">Clear or adjust the filters to see available examples.</p><button onClick={()=>{setQ('');setLevel('All');setFormat('All')}} className="btn-primary mt-5">Clear filters</button></div>}</div></div></div>}
